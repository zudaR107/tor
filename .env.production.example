# Production example - copy to .env and fill in your real domain.
#
# Point DNS A/AAAA records for these six hostnames at this server before
# starting the stack, so Caddy can complete the ACME HTTP-01 challenge and
# get real Let's Encrypt certificates for each of them:
#   example.com
#   auth.example.com
#   kuvert.example.com
#   tafel.example.com
#   zettel.example.com
#   glocke.example.com
DOMAIN=example.com

# schlussel API service (`schlussel`) - CORS allowlist. Named per-service
# (not plain ALLOWED_ORIGINS) because this file provides one shared .env for every
# included service's compose file - see .env.example for why.
SCHLUSSEL_ALLOWED_ORIGINS=https://example.com,https://auth.example.com,https://kuvert.example.com,https://tafel.example.com,https://zettel.example.com,https://glocke.example.com

# kuvert backend service (`kuvert-backend`) - CORS allowlist
KUVERT_ALLOWED_ORIGINS=https://example.com,https://kuvert.example.com

# tafel backend service (`tafel-backend`) - CORS allowlist
TAFEL_ALLOWED_ORIGINS=https://example.com,https://tafel.example.com

# zettel backend service (`zettel-backend`) - CORS allowlist
ZETTEL_ALLOWED_ORIGINS=https://example.com,https://zettel.example.com

# glocke backend service (`glocke-backend`) - CORS allowlist
GLOCKE_ALLOWED_ORIGINS=https://example.com,https://glocke.example.com

# Generate both values independently with `openssl rand -base64 32`.
SCHLUSSEL_TO_GLOCKE_HMAC_KEY_ID=schlussel-v1
SCHLUSSEL_TO_GLOCKE_HMAC_SECRET=replace-with-at-least-32-random-bytes
GLOCKE_TO_SCHLUSSEL_HMAC_KEY_ID=glocke-v1
GLOCKE_TO_SCHLUSSEL_HMAC_SECRET=replace-with-a-distinct-high-entropy-secret

# schlussel frontend service (`schlussel-frontend`) - return_to origin
# allowlist, and where a visitor who
# opened /login or /register directly (no return_to) gets sent instead
ALLOWED_RETURN_ORIGINS=https://example.com,https://kuvert.example.com,https://tafel.example.com,https://zettel.example.com,https://glocke.example.com
DEFAULT_APP_URL=https://example.com

# schloss service (`schloss`) - where its Kuvert/Tafel/Zettel/Glocke cards link to,
# and where its "Войти" button and unauthenticated-visitor redirect point
KUVERT_URL=https://kuvert.example.com
TAFEL_URL=https://tafel.example.com
ZETTEL_URL=https://zettel.example.com
GLOCKE_URL=https://glocke.example.com
SCHLUSSEL_WEB_URL=https://auth.example.com

# `kuvert-frontend`, `tafel-frontend`, `zettel-frontend`, and `glocke-frontend` - where an
# unauthenticated visitor gets redirected to log in (all four reuse the
# same SCHLUSSEL_WEB_URL value as schloss above), and where their header's
# "На главную" link points back to
SCHLOSS_URL=https://example.com
